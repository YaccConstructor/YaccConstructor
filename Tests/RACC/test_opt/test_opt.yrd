{
let value x = x.value

let getStr x y =
    let s = 
        if Option.isSome y
        then " " + y.Value
        else ""
    value x + s 
}

+s: n = NUMBER p = (p = PLUS {value p})? {getStr n p}; 