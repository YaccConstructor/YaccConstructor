//this file was generated by RACC
//source grammar:..\Tests\RACC\test_opt\\test_opt.yrd
//date:2/5/2011 2:46:39 PM

module RACC.Actions_Opt

open Yard.Generators.RACCGenerator

let value x = x.value

let getStr x y =
    let s = 
        if Option.isSome y
        then " " + y.Value
        else ""
    value x + s 

let s0 expr = 
    let inner  = 
        match expr with
        | RESeq [x0; x1] -> 
            let (n) =
                let yardElemAction expr = 
                    match expr with
                    | RELeaf tNUMBER -> tNUMBER :?> 'a
                    | x -> "Unexpected type of node\nType " + x.ToString() + " is not expected in this position\nRELeaf was expected." |> failwith

                yardElemAction(x0)
            let (p) =
                let yardElemAction expr = 
                    match expr with
                    | REOpt(opt) -> 
                        let yardOptAction expr = 
                            match expr with
                            | RESeq [x0] -> 
                                let (p) =
                                    let yardElemAction expr = 
                                        match expr with
                                        | RELeaf tPLUS -> tPLUS :?> 'a
                                        | x -> "Unexpected type of node\nType " + x.ToString() + " is not expected in this position\nRELeaf was expected." |> failwith

                                    yardElemAction(x0)
                                (value p)
                            | x -> "Unexpected type of node\nType " + x.ToString() + " is not expected in this position\nRESeq was expected." |> failwith

                        if opt.IsSome then Some (yardOptAction opt.Value) else None 
                    | x -> "Unexpected type of node\nType " + x.ToString() + " is not expected in this position\nREOpt was expected." |> failwith

                yardElemAction(x1)
            (getStr n p)
        | x -> "Unexpected type of node\nType " + x.ToString() + " is not expected in this position\nRESeq was expected." |> failwith
    box (inner)

let ruleToAction = dict [|("s",s0)|]

