//this file was generated by GNESCC
//source grammar:../../../Tests/GNESCC/test_summator_1/test_summator_1.yrd
//date:10/9/2011 8:01:41 PM

module GNESCC.Regexp_summator_1

open Yard.Generators.GNESCCGenerator
open System.Text.RegularExpressions

let buildIndexMap kvLst =
    let ks = List.map (fun (x:string,y) -> x.Length + 2,y) kvLst
    List.fold (fun (bl,blst) (l,v) -> bl+l,((bl,v)::blst)) (0,[]) ks
    |> snd
    |> dict

let buildStr kvLst =
    let sep = ";;"
    List.map fst kvLst 
    |> String.concat sep
    |> fun s -> ";" + s + ";"

let s childsLst = 
    let str = buildStr childsLst
    let idxValMap = buildIndexMap childsLst
    let re = new Regex("((;2;))")
    let elts =
        let res = re.Match(str)
        if Seq.fold (&&) true [for g in res.Groups -> g.Success]
        then res.Groups
        else (new Regex("((;2;))",RegexOptions.RightToLeft)).Match(str).Groups
    let e0 =
        idxValMap.[elts.[2].Captures.[0].Index] |> RELeaf
    RESeq [e0]
let e childsLst = 
    let str = buildStr childsLst
    let idxValMap = buildIndexMap childsLst
    let re = new Regex("((;6;))|((;6;)(;7;)(;2;))")
    let elts =
        let res = re.Match(str)
        if Seq.fold (&&) true [for g in res.Groups -> g.Success]
        then res.Groups
        else (new Regex("((;6;))|((;6;)(;7;)(;2;))",RegexOptions.RightToLeft)).Match(str).Groups
    if elts.[2].Value = ""
    then
        let e2 =
            let e2 =
                idxValMap.[elts.[6].Captures.[0].Index] |> RELeaf
            let e1 =
                idxValMap.[elts.[5].Captures.[0].Index] |> RELeaf
            let e0 =
                idxValMap.[elts.[4].Captures.[0].Index] |> RELeaf
            RESeq [e0; e1; e2]
        None, Some (e2)
    else
        let e1 =
            let e0 =
                idxValMap.[elts.[2].Captures.[0].Index] |> RELeaf
            RESeq [e0]
        Some (e1),None
    |> REAlt


let ruleToRegex = dict [|(2,e); (1,s)|]

