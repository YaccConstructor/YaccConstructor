{caret}function main(cond)
{
    var query = "a";
    for (var i = 0; i < 10 && i > 0 || query.IsNormalized(); i++)
    {
        var localQ = "b";
        if (cond)
        {
            localQ += "c";
        }
        else
        {
            localQ += "d";
        }
        query += localQ;
    }
    execScript(query);
}