//this file was generated by RACC
//source grammar:W:\Users\gsv2\Diploma\trunk\Tests\RACC\test_alt\\test_alt.yrd
//date:12/8/2010 11:59:42

module RACC.Actions_Alt

open Yard.Generators.RACCGenerator

let value x =
    ((x:>Lexeme<string>).value)

let s0 expr = 
    match expr with
    | REAlt(Some(x), None) -> 
        let yardLAltAction expr = 
            match expr with
            | RESeq [x0] -> 
                let (p) =
                    let yardElemAction expr = 
                        match expr with
                        | RELeaf PLUS -> PLUS :?> 'a

                    yardElemAction(x0)
                box ("Detected: " + (value p))

        yardLAltAction x 
    | REAlt(None, Some(x)) -> 
        let yardRAltAction expr = 
            match expr with
            | RESeq [x0] -> 
                let (m) =
                    let yardElemAction expr = 
                        match expr with
                        | RELeaf MULT -> MULT :?> 'a

                    yardElemAction(x0)
                box ("Detected: " + (value m))

        yardRAltAction x 


let ruleToAction = dict [|("s",s0)|]

